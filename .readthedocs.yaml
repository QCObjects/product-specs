# Read the Docs configuration for QCObjects/product-specs.
# Indexes this repo with MkDocs Material; keep in sync with mkdocs.yml.
version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.12"

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: true

python:
  install:
    - requirements: docs/requirements.txt
